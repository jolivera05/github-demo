---
- name: RestartInstanaAgent
  hosts: all
  tasks:
    - name: restart Instana-Agent
      ansible.builtin.systemd_service:
        name: instana-agent
        state: restarted
        daemon_reload: true
      become: true
      
    
