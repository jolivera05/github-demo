---
- name: RestartInstanaAgent
  hosts: all
  tasks:
    - name: validate Instana-Agent
      ansible.builtin.service:
        name: instana-agent
        state: started        
      become: yes
      
    
